<UserControl x:Class="RoomForRentWindow.AddressesUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vmd="clr-namespace:RoomForRentViewModel;assembly=RoomForRentViewModel"
             xmlns:uc="clr-namespace:RoomForRentWindow.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance Type=vmd:AddressesViewModel}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!--Provinces UserControl-->
        <uc:IdNameModelUserControl Grid.Column="0" DataContext="{Binding ProvincesViewModel}" />

        <!--Districts-->
        <uc:IdNameModelUserControl Grid.Column="1" DataContext="{Binding DistrictsViewModel}" />

        <!--Wards-->
        <uc:IdNameModelUserControl Grid.Column="2" DataContext="{Binding WardsViewModel}" />
    </Grid>
</UserControl>